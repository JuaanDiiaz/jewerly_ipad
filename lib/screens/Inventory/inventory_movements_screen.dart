import 'package:flutter/material.dart';

class InvetoryMovementsScreen extends StatelessWidget {
  const InvetoryMovementsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Inventory Movements'),
      );
  }
}